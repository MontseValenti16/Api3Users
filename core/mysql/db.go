package mysql

import "fmt"

// Simulación de conexión a base de datos (por ejemplo, en memoria)
func InitDB() {
	// Aquí se podría establecer la conexión real o simularla.
	fmt.Println("Conexión a la base de datos simulada iniciada")
}
